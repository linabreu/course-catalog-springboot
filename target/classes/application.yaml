spring:
   datasource:
      username: user
      password: password
      url: jdbc:mysql://localhost:3306/course_catalog
    
   jpa:
      hibernate:
         ddl-auto: create
      show-sql: true
      defer-datasource-initialization: true
   
   sql:
      init:
         mode: always